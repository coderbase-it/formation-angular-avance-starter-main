import { AbstractErrorHandler } from './abstract-error-handler';

describe('AbstractErrorHandler', () => {
  it('should create an instance', () => {
    expect(new AbstractErrorHandler()).toBeTruthy();
  });
});
